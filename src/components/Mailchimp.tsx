"use client";

import React from "react";

// Mailchimp component removed — keeps API compatibility but renders nothing.
export const Mailchimp: React.FC = () => {
  return null;
};

export default Mailchimp;
